# Lab Data Platform：从零开发阶段计划

> 当前执行基准：使用真实神经元 MAT 样例，完成验证入库、查询筛选与授权下载。保留 P01–P14 编号及工作日安排，取消数据生成器和普通文件修订版本流程。字段规则见 [数据契约 v1](docs/data-contract-v1.md)，存储设计见 [数据库设计 v1](docs/database-design-v1.md)。路线图尚未同步的旧描述以本文为准。

编制日期：2026-09-05  
对应路线图：[Lab_Data_Platform_Roadmap.md](Lab_Data_Platform_Roadmap.md)

## 1. 目标与时间口径

从零建立一个可运行、可测试、可自部署的实验数据管理平台。旧代码基本不可用，不依赖旧数据库；使用现有 spatial_data_pre_training 中的真实 MAT sample data 搭建和填充数据库，不开发 fake data generator。

第一条完整流程：登录 → 选择有权限的项目 → 上传 MAT 文件 → 验证并解析 → 去重入库 → 查看导入报告 → 查询筛选 → 授权下载原文件 → 用户本地分析。

当前进度：P01 已形成数据契约与数据库设计草案，已只读检查三个真实文件；权限归属、接口草案和开发启动方案仍需收口。尚未实现应用代码、数据库迁移或正式入库，不将设计文档视为已完成的工程验收。

- 估算前提：单人开发，每周 5 个工作日，每天约 6 小时有效开发；Day 为相对工作日，不是自然日。
- 每阶段 3–5 天，已包含必要的测试、集成和文档时间；每天最后记录实际结果和阻塞。
- Day 1–50：核心验证版，约 10 周。Day 51–60：可选云托管验证，约 2 周。
- 另外预留 5–10 个工作日缓冲，优先处理集成、认证、异步恢复和部署问题。外部反馈等待不计入开发工时。
- 若每天投入约 3 小时，日历周期大致翻倍；根据前两个阶段的实际进度重新估算。
- 阶段以验收通过为完成标准；未通过就修复并更新后续日期，不靠缩减关键验证来维持排期。

## 2. 全程阶段安排

| 阶段 | 工作日 | 天数 | 对应里程碑 | 核心交付与验收 |
|---|---|---:|---|---|
| P01 场景、模型与数据规范 | Day 1–3 | 3 | M0 | 确定实体、MAT 契约和预期报告，能说明一条完整流程 |
| P02 工程骨架与 MAT 解析验证器 | Day 4–7 | 4 | M1 | 前后端、数据库、迁移、CI 跑通；三个真实样例解析验证通过 |
| P03 登录与项目权限 | Day 8–11 | 4 | M1 | 三类角色和项目授权可用，越权访问被拒绝 |
| P04 实验、元数据与文件 | Day 12–16 | 5 | M1 | 真实 MAT 去重入库、查询筛选和授权下载可用；读回与源文件一致 |
| P05 导入与查询下载完整闭环 | Day 17–21 | 5 | M1 | worker 执行导入验证，界面串联上传、报告、查询和下载 |
| P06 任务幂等、重试与恢复 | Day 22–26 | 5 | M2 | 重复投递、worker 中断、存储故障不会产生错误成功状态 |
| P07 追溯与文件一致性 | Day 27–30 | 4 | M2 | 原始文件不可变，数据库与文件一致，导入来源及每次尝试可追溯 |
| P08 自部署与初始化 | Day 31–34 | 4 | M3 | 新环境按文档启动，登录、导入、查询和下载全程通过 |
| P09 备份、恢复与升级 | Day 35–38 | 4 | M3 | 恢复后权限、文件和结果关联正确；迁移与失败恢复演练通过 |
| P10 综合验证与性能基线 | Day 39–43 | 5 | M5 | 有带环境和数据规模的权限、故障与性能报告 |
| P11 修复、文档与演示 | Day 44–47 | 4 | M5 | 解决关键问题，形成教程、架构说明和可重复演示 |
| P12 核心版本验收与发布准备 | Day 48–50 | 3 | M5 | 从干净环境复现同一版本的安装、测试和演示 |
| P13 可选云环境与存储 | Day 51–55 | 5 | M4 | 预算确定后验证云部署、S3 适配器与文件生命周期 |
| P14 可选托管运维与试用 | Day 56–60 | 5 | M4 | 监控、故障演练与成本记录；有伙伴时收集外部试用证据 |

三个检查点：Day 21 能完整演示 MVP；Day 38 能自部署并恢复数据；Day 50 有可复现的核心验证版本。云服务和外部实验室不是这三个检查点的依赖。

## 3. 每阶段具体工作

### P01｜Day 1–3：先定义要验证什么

- Day 1：确定真实神经元 MAT 文件导入场景、用户操作流程和范围；写出成功与失败验收样例。
- Day 2：设计 ResearchAnimal、RecordingSession、NeuronRecording、Trial、SourceFile、ImportRecord；补充 Project、Membership 与 recording 的归属关系及项目权限矩阵。动物/session 可跨项目共享身份，但查询必须按获授权的 recording 过滤；神经元编号仍为实验室全局唯一。
- Day 3：定义 MAT 字段契约、字段映射、验证报告格式；确定本地存储优先、OIDC 身份提供方及开发启动方案，记录关键架构决策。

交付：范围说明、实体图、接口草案、真实样例数据规范与预期验证结果。验收：无需旧系统就能解释每一步输入、存储位置、权限、输出和失败行为。

### P02｜Day 4–7：搭好可持续开发的基础

- Day 4：建立前后端目录、配置示例和启动说明；核实并锁定依赖版本。
- Day 5：连接 PostgreSQL，实现核心数据库模型及首次 Alembic 迁移，落实外键、animal/session 组合唯一、神经元编号和文件校验和唯一约束；建立 API 健康检查。
- Day 6：实现 MAT 解析与验证器，用已检查的三个真实样例验证字段、矩阵和 trial 对应关系；错误测试使用临时副本，不修改原样例。
- Day 7：接入基础 CI、后端测试和开发用 Compose；在空数据库上验证启动和迁移。

验收：按说明能启动前后端并连接数据库；三个真实样例解析结果符合数据契约；原始文件校验和保持不变；CI 运行有效的基础检查。

### P03｜Day 8–11：身份和项目权限

- 接入选定的可自部署 OIDC 服务，完成登录、退出和开发演示账号配置。
- 实现用户映射、项目创建、成员管理与管理员／研究人员／只读角色。
- 将授权检查落实在 API；定义查询、导入、报告查看和下载权限；落实 recording 的项目归属，不通过全局 animal/session 列表泄露未授权记录。
- 验证未登录、只读写入、跨项目读取、直接通过资源 ID 访问等场景。

验收：两个隔离项目及三个角色可以演示；用户无法绕过前端直接通过 API 访问其他项目的资源。认证部署较难时优先解决，不把前端隐藏按钮当作授权完成。

### P04｜Day 12–16：实验记录与文件落地

- 实现受项目权限控制的导入接口，复用 P02 验证器；从文件提取 animal、session、神经元、实验分类及 trial 信息。
- 完整 raster 保存在受控本地 MAT 原文件中；PostgreSQL 保存可查询字段、trial 标签/时间、文件路径关联与 SHA-256。保留原始名称，内部路径使用系统 ID。
- 拒绝重复 SHA-256 或全局神经元编号；不覆盖旧记录、不提供普通修订版本入口。仅改文件名无法绕过去重；特殊重新编号须内外身份一致。
- 实现事务回滚、临时文件清理、并发唯一约束和成功前读回核对。基本数据正确性在本阶段验收，不推迟到 P06/P07。
- 实现按 animal、session、神经元编号、实验类型、训练阶段、脑区查询筛选、分页和详情；下载通过后端授权，返回原始文件名及校验和。
- 三个文件通过验收后，再按文件批量导入现有 `_1_` 样例；每份文件独立提交，返回成功、拒绝或跳过报告。

验收：空测试库导入三个样例后有 3 个 animal、3 个 session、3 个 recording、3 份源文件和 432 个 trial；所有已映射字段、完整 raster 和原文件校验和读回一致。重复/改名上传业务数据数量不变；失败不产生不完整记录；下载字节与原文件一致；未授权访问被拒绝。

### P05｜Day 17–21：第一个端到端 MVP

- 接入 Celery、Redis 和独立 worker，将 P04 导入服务接入后台执行，避免维护第二套导入逻辑。
- 持久化导入状态，区分验证拒绝、范围外跳过、重复数据、系统失败和成功。
- 界面串联项目选择、上传、报告、筛选、详情与授权下载；报告定位到字段和适用的 trial/列号。
- 界面展示来源文件、解析器/契约版本和校验和；不要求在线 spike rate 计算、事件对齐或科学分析。
- 用 Playwright 验证完整流程，包括重复拒绝与越权拒绝。下载的是独立副本，本地修改不回写平台。

验收：真实样例完成登录到下载的全流程，不需要手工改库；临时错误夹具给出预期报告。基本回滚与一致性已通过，进一步的中断恢复在 P06/P07 验证。

### P06｜Day 22–26：任务可靠性

- 明确导入状态转换、每次执行尝试、超时和错误分类；永久性输入错误不盲目重试，暂时性故障有限重试。
- 实现提交幂等键、重复消息防护及数据库提交与队列发布之间的可靠投递。
- 区分同一任务的恢复和用户再次上传：恢复不重复创建业务记录，再次上传已存在数据返回重复提示。
- 使用执行租约或等效机制防止旧 worker 重复提交；识别失联导入并记录恢复过程。
- 演练重复提交、并发同一编号、重复投递、入队失败、worker 中断和存储不可写。

验收：同一文件或神经元最多一份有效业务记录；没有永久挂起且无法识别的任务；暂时故障可恢复，永久错误有原因和终态。

### P07｜Day 27–30：追溯与文件一致性

- 记录源文件 SHA-256、原始名称、上传者、导入时间、解析器/契约版本及每次尝试；系统时间不冒充采集时间。
- 深入验证文件发布与数据库提交之间的失败边界；提交结果不明确时先查数据库，不直接删除文件。
- 实现失效导入恢复和孤立文件核对，确认没有活跃导入或成功记录引用后才清理；原文件不可覆盖。
- 核对完整矩阵、trial 顺序、标签和时间值；检查丢失或损坏文件并报告，不把损坏状态当作有效下载。
- 补充来源与导入详情、下载审计；重新执行只读核验不覆盖已有业务数据。

验收：每条 recording 可追溯到原文件与导入过程；半成品不可下载；崩溃恢复不误删成功文件、不暴露无效记录；发现文件缺失/校验和不符时下载明确失败。

### P08｜Day 31–34：可自部署包

- 完善带版本号的镜像构建和 Compose，覆盖 API、前端、数据库、Redis、worker、认证服务。
- 提供配置示例、管理员初始化、持久化卷、健康检查和明确迁移入口。
- 写出安装、HTTPS、密钥配置和常见故障说明。
- 在独立的干净环境安装，执行 MVP 全流程并记录问题。

验收：不依赖开发机绝对路径或残留数据即可运行。能邀请他人安装时记录实际反馈；只能自行复现时如实记录，不标记为他人验收。

### P09｜Day 35–38：数据可以救回来

- 首版采用暂停写入、等待／停止任务后配套备份数据库与文件的明确流程，并覆盖认证配置或身份数据依赖。
- 提供备份清单、校验和、恢复脚本与演练说明。
- 在新环境恢复项目、成员权限、animal/session、神经元和 trial 记录、原始文件及导入报告；核对数量、校验和与关联关系。
- 演练一次包含实际 schema 变化的版本升级，以及升级失败后从完整备份恢复。

验收：恢复后的完整业务流程可用；备份中没有缺少文件的有效记录；记录停机、备份和恢复耗时。恢复目标先测量，再写成有依据的指标。

### P10｜Day 39–43：证明系统行为

- 执行完整权限矩阵、MAT 字段校验、导入追溯、重复文件/编号、重复任务和恢复验证。
- 分批使用现有有效文件，记录实际文件大小与 trial 数；增加查询/下载并发，测量 API 延迟、导入吞吐、任务排队时间、内存及磁盘占用。重新测量导入使用独立空测试库，不伪造编号绕过去重。
- 记录硬件、操作系统、软件版本、样例清单与校验和、实际数据规模、并发和运行命令。
- 找出主要瓶颈，安排必要修复；根据测量结果声明支持范围和限制。

验收：报告可复跑，失败与边界均有记录；指标来自运行结果，不能由假数据规模推算成真实用户规模。

### P11｜Day 44–47：整理成别人看得懂的项目

- 修复影响数据正确性、权限、安装和主流程的问题，重跑对应验证。
- 完善架构图、设计取舍、API 说明、安装教程、样例导入说明和故障处理手册。
- 编排 5–10 分钟演示：正常导入、重复拒绝、查询下载、来源追溯和任务恢复。
- 整理测试与运行证据、已知限制和后续任务。

验收：新读者能理解问题、运行系统并验证关键能力；示例、截图和文档准确标明真实样例来源；对外展示前另行确认数据使用范围。

### P12｜Day 48–50：核心版本收口

- 从干净环境按候选版本执行安装、迁移、样例导入、端到端演示和恢复检查。
- 确保代码版本、镜像标识、文档和演示一致；形成版本说明与检查记录。
- 制作可发布的软件包／镜像构建产物；发布位置确定后执行相应发布。

验收：交付一个可复现的核心版本。无外部实验室参与时，描述为“真实样例验证完成”，不描述为生产落地或外部试点成功。

### P13｜Day 51–55：可选云部署

进入条件：核心版本通过验收，确有托管需求，云账号、区域与预算已确定。

- 核实云服务规格与费用，确定容量、清理方式和预算提醒。
- 实现并验证 S3 存储适配器、授权上传下载、完整性检查和本地相同的生命周期规则。
- 根据文件规模决定是否引入分片上传；通过 Terraform 建立独立试点环境并部署。

验收：云上流程通过，上传授权不会越过项目边界，资源可以重复创建和清理，有实际资源与成本记录。

### P14｜Day 56–60：可选托管运维与试用

- 验证延迟、任务积压、失败率、存储增长及备份状态的监控和告警。
- 演练云上失败任务定位、备份恢复和容量限制。
- 有试用者时安排独立实例、使用教程并收集反馈；无人试用时执行本地真实样例场景演练。

验收拆开记录：云部署验证、运维演练、外部试用。前两项通过不等于第三项完成；外部反馈需要额外等待或修复时另排计划。

## 4. 真实 sample data 的使用方式

不开发 fake data generator，不生成合成传感器数据。使用 `spatial_data_pre_training` 中现有 MAT 文件作为解析验证、数据库填充及查询下载验收的数据来源。

- P02：先使用已检查的 ADR001_1_3000、ADR004_1_3007、ELV003_1_1004 三个真实文件，预期合计 432 个 trial；本阶段验证解析和建表，不执行正式业务导入。
- P04：导入流程通过读回一致性与去重验收后，批量导入现有样例；只处理指定位置为 `_1_` 的文件，逐文件报告成功、拒绝或跳过。
- 原始样例只读使用，不重命名、不改编号、不修改内容。系统保存独立的受控原文件副本。
- 错误与边界测试在临时副本或内存中构造字段缺失、长度不符等情况，仅用于验证器测试，不作为真实数据填充数据库，不需要独立生成器。
- 性能测试按实际有效文件数和 trial 数报告，不通过重复导入或伪造神经元编号扩充数据量。
- 真实样例不自动上传、发布或提交公共仓库。用户下载独立副本进行本地分析，不回写平台原始记录。

## 5. 范围与执行纪律

Day 1–50 优先使用真实 MAT 样例、一个导入验证流程和本地文件存储；首版采用固定 MAT 契约，不开发通用 Schema 配置或编辑器。沿用 roadmap 的模块化单体、独立 worker 与项目级权限方向，实际依赖版本在搭建时核实。

暂不开发仪器控制、任意代码上传执行、通用工作流编辑器、计费、多租户 SaaS、Kubernetes。额外图表、更多数据格式和云服务优先排在核心验收之后。

每阶段结束记录：交付版本、演示步骤、测试结果、未解决问题、实际工时和下一阶段调整。涉及数据丢失、越权、错误成功状态或无法安装的问题必须修复后再通过对应验收。

所有新增实现作为离职后独立项目记录。项目可以展示架构、权限、数据一致性和工程验证能力；真实使用人数、采用情况与性能指标只按实际证据描述。
