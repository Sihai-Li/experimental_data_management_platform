# Local development only. Run tools from the repository root.
DATABASE_URL=postgresql+psycopg://lab:lab_dev_only@localhost:5432/lab_platform
POSTGRES_USER=lab
POSTGRES_PASSWORD=lab_dev_only
POSTGRES_DB=lab_platform
DATA_ROOT=./storage
# P03: configure after Keycloak is introduced.
# OIDC_ISSUER=http://localhost:8080/realms/lab
# OIDC_CLIENT_ID=lab-platform
