# 实验室数据平台开发路线图

> 当前范围更新：首个里程碑使用已有真实神经元 MAT 样例，完成验证、不可变保存、去重和读回一致性核对。当前规则见 [数据契约 v1](docs/data-contract-v1.md) 与 [数据库设计 v1](docs/database-design-v1.md)。下文原有 CSV、合成传感器及文件版本流程属于待调整的历史规划。

记录日期：2026-09-05  
状态：采用现有真实 MAT 样例；取消 fake data generator，保留原阶段编号  
项目名称：暂定 Lab Data Platform

执行计划见 [Lab_Data_Platform_Execution_Plan.md](Lab_Data_Platform_Execution_Plan.md)。当前按单人全职估算，以每阶段 3–5 个工作日推进；核心验证版约 50 个工作日，可选托管阶段另需约 10 个工作日。

## 1. 产品目标

基于过去实验室数据管理与处理场景的经验，从零开发一个独立的新平台，逐步使其他实验室可以自行部署、升级维护，也可以使用托管实例。当前旧代码基本不可用，不依赖旧系统代码、数据库或处理脚本；使用已经找回的真实 MAT 样例。

当前优先目标是使用 spatial_data_pre_training 中的真实 MAT 样例搭建和填充数据库，验证解析、去重入库、查询筛选、授权下载及故障恢复。不开发 fake data generator；真实样例验证不代表生产采用或科学分析有效性。

第一版面向有大量实验文件、需要管理元数据、多人协作并运行 Python 处理流程的研究实验室。“任何实验室都能使用”是长期目标，第一版优先验证相近场景，通过可配置字段和处理流程逐步扩展。

核心使用流程：

> 创建项目 → 登记实验与元数据 → 上传数据 → 执行校验和处理 → 查看状态与结果 → 下载或追溯结果来源。

工程目标：别人能够安装和使用；开发者能够测试、部署、排错、备份、恢复和升级。

## 2. 第一版范围

| 模块 | 第一版内容 |
|---|---|
| 项目与实验 | Project、Experiment、Dataset；固定核心字段及可配置元数据 |
| 文件管理 | 上传、下载、校验、版本、文件与实验关联 |
| 成员与权限 | 管理员、研究人员、只读用户；项目级访问控制 |
| 处理任务 | 预定义 Python 流程；状态、日志、失败原因、有限重试 |
| 结果追溯 | 输入文件版本、运行参数、程序版本、输出文件 |
| 部署维护 | 安装、配置、备份、恢复、升级 |

暂不纳入第一版：实验仪器控制、通用工作流拖拽编辑器、计费系统、任意用户代码执行、共享数据库的多租户 SaaS、大规模微服务拆分与 Kubernetes 集群。

## 3. 推荐技术栈

以下为从零开发的规划选型。实施前核实受支持版本并锁定依赖，不安排旧 Flask/MySQL 实现的迁移工作。

| 层次 | 推荐选择 | 用途与引入时机 |
|---|---|---|
| 前端 | React、TypeScript、Vite | MVP：管理界面，无明确 SSR 需求 |
| 后端 | FastAPI、Pydantic | MVP：REST API、输入验证、接口契约 |
| 数据访问 | SQLAlchemy、Alembic | MVP：模型、事务、数据库迁移 |
| 数据库 | PostgreSQL | MVP：元数据、权限、任务和审计记录 |
| 文件存储 | 本地文件系统适配器、S3 适配器 | MVP 先实现一种；自部署与托管阶段验证另一种 |
| 异步处理 | Celery、Redis | MVP 引入基础任务，可靠性阶段完善恢复机制 |
| 身份认证 | OIDC，接入成熟身份提供方 | MVP 确定可自部署方案；应用负责项目级授权 |
| 测试 | pytest、Playwright | MVP 开始：后端与关键用户流程 |
| 打包交付 | Docker Compose、GitHub Actions | 开发期即使用；自部署阶段完善发布 |
| 可观测性 | 结构化日志，后续 OpenTelemetry | 先记录请求与任务关联 ID，再接入遥测 |
| 云托管 | AWS ECS/Fargate、RDS、S3、托管 Redis | 托管试点阶段，先估算成本 |
| 基础设施配置 | Terraform | 托管阶段：重复创建和维护环境 |

架构采用模块化单体后端和独立 worker 进程。API 与 worker 共享代码库及发布版本，可以独立运行和扩容。

## 4. 数据与计算设计

| 组件 | 职责 |
|---|---|
| PostgreSQL | 元数据、权限、文件位置、任务状态、数据来源关系 |
| 文件存储 | 原始数据、处理结果、大型数组或影像文件 |
| Worker | 校验、转换、统计等耗时任务 |

核心实体：

- `Project → Experiment → Dataset → FileVersion`
- `PipelineDefinition → PipelineRun → OutputArtifact`

每次运行记录输入版本、参数、代码版本与结果。原始大文件通常不直接存入关系数据库。

云端大文件上传采用 API 授权后直传对象存储；根据文件大小引入分片上传、进度记录与完整性校验。本地存储路径遵守同样的文件生命周期和权限规则。

通用元数据采用固定核心字段加 JSON Schema 定义的扩展字段，暂不提供任意数据库结构编辑器。

初期一个实验室对应一个部署实例；托管试点也优先采用独立实例与独立存储。跨实例共享数据库与多租户隔离另行设计。

## 5. Milestone 总览

时间为单人开发的全职等效估算，并非已承诺的日历期限。核心验证版约 50 个工作日（10 周），可选托管阶段约 10 个工作日（2 周）；另建议预留 5–10 个工作日处理未知问题。按阶段验收结果调整，不依赖旧代码复用。执行顺序为 M0 → M1 → M2 → M3 → M5 → 可选 M4，先发布可复现的本地验证版。

| Milestone | 估算 | 交付物 | 验收标准 |
|---|---:|---|---|
| M0：需求与从零设计 | 3 天 | 产品范围、实体草图、真实 MAT 数据契约 | 明确一条可验证流程及预期结果 |
| M1：可用 MVP | 2–3 周 | 项目、元数据、文件、登录权限、一个处理流程 | 上传到结果下载全程可用 |
| M2：可靠的数据处理 | 2–3 周 | 状态机、重试、幂等、版本与追溯 | 重复提交和 worker 中断不会产生错误结果 |
| M3：可自部署版本 | 2 周 | Compose、安装、备份恢复、升级与运维说明 | 他人能在干净服务器安装并恢复备份 |
| M4：托管试点（可选） | 约 2 周，外部等待另计 | 云部署、监控、资源限制、运维文档 | 云部署演练通过；外部试用另行验收 |
| M5：验证与发布 | 1–2 周 | 性能报告、故障测试、文档、演示、版本发布 | 发布结果可复现，有测试与使用证据 |

M4 的外部试用取决于试点伙伴；提前联络。若暂时没有伙伴，内部模拟验收只能作为替代验证，不标记外部试点完成。

## 6. 各阶段任务

### M0：需求与从零设计

- 选择一个场景：上传真实 MAT 文件，解析元数据并运行一致性检查，与同组成员共享结果。
- 从使用场景重新设计数据模型、接口、处理脚本和部署方式。
- 使用三个已检查的真实 MAT 样例作为正确性基准；错误及边界测试仅操作临时副本或内存，不开发数据生成器。
- 将路径、用户、实验室名称等设计为配置或数据库记录。
- 确定核心实体、用户角色、第一版支持的数据格式与文件规模。
- 记录架构决策及验收脚本，建立开发仓库和任务清单。

验收：一份范围说明、一份实体与流程草图、一组真实 MAT 样例及预期校验结果。

### M1：可用 MVP

- 实现项目创建、成员权限、实验登记和元数据验证。
- 实现文件上传下载及数据库关联。
- 接入后台任务，运行一个真实有用的质量检查流程。
- 输出必需字段、格式、缺失值、范围检查等质量报告。
- 提供任务状态和结果下载界面。
- 建立 CI，执行核心测试。

关键测试：跨项目访问被拒绝；非法元数据被拒绝；文件与记录匹配；完整流程生成可下载结果。

验收：使用样例数据，从新用户进入系统到获取结果完整演示；自动化测试通过。

### M2：可靠的数据处理

- 明确定义待运行、运行中、成功、失败及恢复相关状态转换。
- 使用幂等键处理重复提交与消息重复投递。
- 临时故障有限重试，错误输入直接返回可理解的原因。
- 识别 worker 中断后的遗留任务，支持恢复或安全重跑。
- 输出先写入临时位置，校验成功后再标记可用。
- 记录每次尝试的日志、错误、时间及输入输出版本。
- 限制任务运行时间和资源使用。
- 处理流程仅允许管理员注册，记录流程接口及依赖版本。

验收：测试重复提交、处理进程中断、存储故障及非法数据，确认没有重复有效结果或错误成功状态，并可追溯每次运行。

### M3：可自部署版本

- 发布带版本号的镜像、Compose 配置和配置示例。
- 实现首次管理员初始化、健康检查与持久化配置。
- 提供 HTTPS、认证服务、存储及密钥配置说明。
- 提供显式数据库迁移步骤。
- 定义数据库与文件配套备份的一致性方法。
- 提供备份、恢复、升级前检查和升级失败恢复流程。
- 编写安装、运维、排错文档。
- 请未参与开发的人在干净环境执行安装，并恢复一份备份。

验收：记录安装问题与实际耗时；备份恢复后权限、元数据、文件及结果关联正确；验证一次版本升级。

### M4：托管试点

- 估算基础设施成本并确定试点容量与预算。
- 使用 Terraform 定义云环境，以 ECS/Fargate、RDS、S3 和托管 Redis 部署。
- 为试点实验室提供独立实例和存储配置。
- 监控 API 延迟、任务积压、失败率、存储增长、备份状态。
- 用关联 ID 串联 API 请求和后台任务；按需要引入 OpenTelemetry。
- 记录存储量、计算耗时与下载流量，评估托管成本。
- 提供运维操作手册，处理一次实际或演练故障。
- 收集外部实验室对安装、使用、结果和维护的反馈。

验收：外部实验室完成真实流程，反馈已记录；能发现并定位失败任务；成本与维护责任明确。

### M5：验证与发布

- 在注明硬件、数据量、文件大小和并发条件下执行性能测试。
- 验证权限边界、任务恢复、数据一致性、备份恢复和升级流程。
- 整理架构图、设计决策、API 文档、教程和演示。
- 完善发布说明、已知限制、支持环境和版本策略。
- 发布可复现的版本，记录真实采用情况及后续改进清单。

验收：仓库、镜像、文档和演示对应同一发布版本；测试与指标有来源；已知限制明确。

## 7. 阶段计划制定方式

以执行计划中的 3–5 个工作日阶段为推进单位，每阶段以一个可验收的结果为中心，控制任务数量，并预留集成、排错和文档时间。阶段未通过验收时，优先补齐，不按日历强行推进。以下周记录模板可以用于复盘，不替代阶段验收。

每周计划模板：

```markdown
### Week N：本周目标
- 所属 Milestone：
- 可投入时间：
- 本周可演示结果：
- 核心任务（建议 3–5 项）：
- 验收方式与必要测试：
- 依赖与待确定事项：
- 本周不做的内容：
- 实际完成情况与证据：
- 下周调整：
```

已确认：在当前项目目录从零开发，使用现有真实 MAT 样例验证和填充数据库，不开发 fake data generator。执行计划暂按每周 5 个工作日、每天约 6 小时有效开发时间，首个格式采用 MAT；实际投入时间与格式可在第 1 阶段调整。外部试用者不是核心验证版的前置依赖。

## 8. 求职展示与经历边界

每个 Milestone 保留可展示证据：架构决策、可运行代码、样例数据、测试、性能报告、部署与恢复记录、用户反馈。

可以重点讲述：文件与元数据分离、权限设计、重复任务处理、失败恢复、数据追溯、部署升级与性能取舍。

UChicago 当年实际完成的工作继续归属于原工作经历；现在新开发的通用平台作为独立的后续项目记录日期与贡献。简历中的用户数、性能与可靠性指标只使用真实验证结果。

## 9. 技术参考

- FastAPI 容器部署：https://fastapi.tiangolo.com/deployment/docker/
- React 构建应用：https://react.dev/learn/build-a-react-app-from-scratch
- Docker Compose 生产部署：https://docs.docker.com/compose/how-tos/production/
- Celery 任务与重试：https://docs.celeryq.dev/en/stable/userguide/tasks.html
- S3 预签名 URL：https://docs.aws.amazon.com/AmazonS3/latest/userguide/using-presigned-url.html
- S3 分片上传：https://docs.aws.amazon.com/AmazonS3/latest/userguide/mpuoverview.html
- ECS/Fargate：https://docs.aws.amazon.com/AmazonECS/latest/developerguide/getting-started-fargate.html
- OpenTelemetry：https://opentelemetry.io/docs/what-is-opentelemetry/

参考资料核查于路线图讨论期间；实施时重新核实版本、兼容性与运行成本。
